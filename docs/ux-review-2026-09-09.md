# Well learned milestone verification

Story: owned skill overview → current-scope evidence and schedule queries → the
mastery-v1 projection → a concise next step and expandable checklist. A saved recall
also persists the same projection's milestone as historical feedback.

## What changed

- One shared panel shows the next unmet requirement; the eight gates are collapsed
  by default. Icons have screen-reader text, and disclosure works with the keyboard.
- A stage transition into Well learned gets a quiet achievement message. Existing
  Well learned skills explain why they earned it; overdue skills explain Refresh due
  without suggesting their evidence has disappeared.
- The exact same gate booleans drive the mastery decision and checklist. Existing
  thresholds, scheduling, and stored learning evidence are unchanged.
- The overview recomputes an owner-checked current-scope projection. Historical
  recall results retain their snapshot and are labelled accordingly. Legacy results
  without milestone data render without fabricated progress.

## Evidence and limits

- Local Idempotent operations loaded as Demonstrated. Seven gates were met; transfer
  showed zero qualifying question families despite two transfer sessions/days. The
  UI accurately identified different new-angle challenges as the remaining evidence.
- Desktop layout was inspected. At 320px, collapsed and expanded document width
  remained 320px. Enter opened/closed the disclosure; no browser errors or warnings
  were captured in the inspected tab.
- Server startup succeeded and the authenticated skill route returned 200. The page
  rendered actual database-derived counts, not a frontend fixture.
- Policy and renderer tests cover achieved, refresh due, each failed gate, empty
  scope, no automatic progress from waiting, old summaries, and historical wording.
  Query tests cover ownership, unsupported policy versions, and stored evidence mapping.
- Full automated browser coverage and a live milestone-earning recall were not run.
  No existing learning data was mutated to manufacture an achievement for screenshots.

Next at this checkpoint: cited repair access and direct recall navigation (completed below), then public-demo alignment.
No new dependencies or migration were needed for this slice. The prior quiz migration
and quiz-feedback work remain unpushed alongside these changes.

## Follow-up: cited repair and recall access

Story: Today or a skill overview → cited repair → same skill or selected recall,
without hiding due work, changing scheduling, or awarding assisted-learning credit.

- Today retains its repair recommendation and names the earliest due skill as a
  secondary recall action. Existing owned/live candidate filtering and active-session
  conflict handling are reused. No additional database query or write is introduced.
- Skill pages expose Recall now beside Strengthen, or Recall schedule when current.
  Selected recall pages include an exit to that skill and do not offer early review.
- Repair shows the saved source passage and locator immediately. Its example stays
  collapsed; removing autofocus keeps the page at the reading content. Website links
  open separately, announce the new tab, and accept only HTTP(S) URLs.
- Completion keeps the assisted-practice explanation and links back to the skill or
  its recall view. Authentication recovery now preserves the exact repair URL.

Verification: the running local app showed Today’s Arrays repair alongside the due
Hashmaps recall; opening repair displayed the stored array-notes passage without a
disclosure. Back to skill returned to Arrays, where Recall now opened its own due
review form. Idempotent operations’ future recall showed its date and Back to skill,
not an early-start form. At 320px the repair document width was 320px and focus stayed
on BODY on entry; Enter expanded/collapsed the example. Desktop recall layout was
inspected at 1280px and browser warning/error logs were empty. Viewport override was
reset. No repair submission, recall session creation, or evidence mutation was made.

Unit/render coverage includes navigation branches, source-link safety, visible
citations, and completed repair destinations. Production build passed. The e2e
learning-loop script now expects the visible source passage and selected recall
return; the complete browser suite was not run. Next: public-demo alignment.
